# Техническое задание: Фронтенд Hearty.pro

## 📋 Оглавление

1. [Обзор проекта](#обзор-проекта)
2. [Архитектура фронтенда](#архитектура-фронтенда)
3. [Модуль 1: Analytics Dashboard](#модуль-1-analytics-dashboard)
4. [Модуль 2: Admin Dashboard](#модуль-2-admin-dashboard)
5. [Модуль 3: Real-time Features](#модуль-3-real-time-features)
6. [Модуль 4: UI/UX Improvements](#модуль-4-uiux-improvements)
7. [Технический стек](#технический-стек)
8. [API Integration](#api-integration)
9. [Приоритеты и сроки](#приоритеты-и-сроки)

---

## Обзор проекта

**Hearty.pro** — платформа для поиска и бронирования услуг психологов и коучей.

### Текущее состояние фронтенда

✅ **Реализовано:**
- Главная страница с AI-чатом
- Каталог специалистов с фильтрами и сортировкой
- Профили специалистов
- Система бронирования (3-шаговый stepper)
- Онбординг для специалистов
- Skeleton loaders и error states
- Базовый layout с навигацией

### Что нужно добавить

На бэкенде готовы 4 новых модуля API:
1. **Analytics API** — 13 эндпоинтов для статистики
2. **Admin Dashboard API** — ~20 эндпоинтов для администрирования
3. **WebSocket API** — real-time функциональность
4. **Integration Tests** — покрытие всех endpoints

**Задача:** Создать фронтенд-интерфейсы для интеграции с этими API.

---

## Архитектура фронтенда

### Структура директорий

```
src/
├── components/
│   ├── analytics/          # Новые компоненты аналитики
│   │   ├── SpecialistDashboard.tsx
│   │   ├── EarningsChart.tsx
│   │   ├── BookingsTimeline.tsx
│   │   ├── RatingsBreakdown.tsx
│   │   ├── PopularTimes.tsx
│   │   └── ClientInsights.tsx
│   ├── admin/              # Новые компоненты админки
│   │   ├── AdminLayout.tsx
│   │   ├── UsersTable.tsx
│   │   ├── BookingsTable.tsx
│   │   ├── FinancialReports.tsx
│   │   ├── ModerationQueue.tsx
│   │   └── PlatformStats.tsx
│   ├── chat/               # Real-time чат
│   │   ├── ChatWindow.tsx
│   │   ├── ChatList.tsx
│   │   ├── MessageBubble.tsx
│   │   └── TypingIndicator.tsx
│   ├── notifications/      # Real-time уведомления
│   │   ├── NotificationBell.tsx
│   │   ├── NotificationList.tsx
│   │   └── NotificationItem.tsx
│   └── ui/                 # Переиспользуемые UI компоненты
│       ├── Card.tsx
│       ├── Table.tsx
│       ├── Chart.tsx
│       ├── Badge.tsx
│       ├── Tabs.tsx
│       └── DateRangePicker.tsx
├── hooks/                  # Новые кастомные хуки
│   ├── useAnalytics.ts     # Получение данных аналитики
│   ├── useWebSocket.ts     # WebSocket connection
│   ├── useChat.ts          # Чат функциональность
│   ├── useNotifications.ts # Real-time уведомления
│   └── useAdmin.ts         # Админ функции
├── services/               # API clients
│   ├── api.ts              # Базовый HTTP client
│   ├── analytics.ts        # Analytics API
│   ├── admin.ts            # Admin API
│   ├── socket.ts           # Socket.IO client
│   └── auth.ts             # Аутентификация
├── types/                  # TypeScript типы
│   ├── analytics.ts
│   ├── admin.ts
│   ├── chat.ts
│   └── api.ts
├── utils/
│   ├── charts.ts           # Утилиты для графиков
│   ├── formatters.ts       # Форматирование данных
│   └── validators.ts       # Валидация форм
└── App.tsx                 # Главный компонент (обновить routes)
```

### Новые routes

```typescript
// В App.tsx добавить:
<Routes>
  {/* Существующие routes */}
  <Route path="/" element={<Home />} />
  <Route path="/specialists" element={<Specialists />} />
  <Route path="/specialist/:id" element={<SpecialistProfile />} />
  <Route path="/booking/:id" element={<Booking />} />
  <Route path="/onboarding" element={<Onboarding />} />

  {/* НОВЫЕ routes для специалистов */}
  <Route path="/dashboard" element={<ProtectedRoute role="SPECIALIST" />}>
    <Route index element={<SpecialistDashboard />} />
    <Route path="analytics" element={<AnalyticsPage />} />
    <Route path="bookings" element={<MyBookingsPage />} />
    <Route path="earnings" element={<EarningsPage />} />
    <Route path="clients" element={<ClientsPage />} />
    <Route path="chat" element={<ChatPage />} />
  </Route>

  {/* НОВЫЕ routes для админов */}
  <Route path="/admin" element={<ProtectedRoute role="ADMIN" />}>
    <Route index element={<AdminOverview />} />
    <Route path="users" element={<UsersManagement />} />
    <Route path="users/:id" element={<UserDetails />} />
    <Route path="specialists" element={<SpecialistsModeration />} />
    <Route path="reviews" element={<ReviewsModeration />} />
    <Route path="bookings" element={<BookingsManagement />} />
    <Route path="financials" element={<FinancialReports />} />
    <Route path="analytics" element={<PlatformAnalytics />} />
  </Route>

  {/* НОВЫЕ routes для клиентов */}
  <Route path="/client" element={<ProtectedRoute role="CLIENT" />}>
    <Route path="bookings" element={<MyBookings />} />
    <Route path="chat" element={<ClientChat />} />
    <Route path="notifications" element={<Notifications />} />
  </Route>
</Routes>
```

---

## Модуль 1: Analytics Dashboard

### 1.1. Specialist Analytics Dashboard

**Route:** `/dashboard/analytics`

**API Endpoints:**
- `GET /api/analytics/specialist/:id/dashboard` - общая статистика
- `GET /api/analytics/specialist/:id/earnings` - заработок
- `GET /api/analytics/specialist/:id/bookings-timeline` - тренды бронирований
- `GET /api/analytics/specialist/:id/ratings-breakdown` - детализация рейтингов
- `GET /api/analytics/specialist/:id/popular-times` - популярные слоты
- `GET /api/analytics/specialist/:id/client-insights` - инсайты по клиентам

#### Компоненты

##### 1.1.1. SpecialistDashboard.tsx

**Макет:**
```
┌─────────────────────────────────────────────────────┐
│ Аналитика                                           │
│ ┌─────────────────┐  ┌─────────────────┐           │
│ │ Период выбора   │  │ Экспорт PDF     │           │
│ └─────────────────┘  └─────────────────┘           │
│                                                     │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐│
│ │Бронирован│ │ Заработок│ │ Сессии   │ │Рейтинг  ││
│ │ 24       │ │ 96,000₽  │ │ 18       │ │ 4.8 ⭐  ││
│ │ +15%     │ │ +12%     │ │ +8%      │ │ +0.2    ││
│ └──────────┘ └──────────┘ └──────────┘ └─────────┘│
│                                                     │
│ ┌─────────────────────────────────────────────────┐│
│ │ 📊 График заработка по дням (Line Chart)       ││
│ │                                                 ││
│ │     80k│         ╱╲                             ││
│ │     40k│    ╱╲  ╱  ╲   ╱╲                       ││
│ │      0 ├────────────────────                    ││
│ │        Пн Вт Ср Чт Пт Сб Вс                     ││
│ └─────────────────────────────────────────────────┘│
│                                                     │
│ ┌────────────────────┐ ┌────────────────────────┐ │
│ │ 📈 Популярные часы │ │ 👥 Инсайты по клиентам││ │
│ │                    │ │                        │ │
│ │ 18:00 - 72 брони   │ │ Всего клиентов: 42    │ │
│ │ 17:00 - 68 брони   │ │ Повторных: 28 (67%)   │ │
│ │ 19:00 - 65 брони   │ │ Средняя сессия: 2.3   │ │
│ └────────────────────┘ └────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**Требования:**
- Фильтр по периоду: "Последние 7 дней", "30 дней", "90 дней", "Год", "Кастомный период"
- Все цифры показывают изменение к предыдущему периоду (стрелки вверх/вниз с процентами)
- Кнопка "Экспорт PDF" для скачивания отчета
- Адаптивный дизайн (мобильная версия: карточки в колонку)
- Skeleton loaders при загрузке данных

**Функционал:**
```typescript
interface SpecialistDashboardProps {
  specialistId: string
}

const SpecialistDashboard: React.FC<SpecialistDashboardProps> = ({ specialistId }) => {
  const [dateRange, setDateRange] = useState<DateRange>({ from: '7days' })
  const { data, loading, error } = useAnalytics(specialistId, dateRange)

  return (
    <div className="analytics-dashboard">
      <DateRangeSelector value={dateRange} onChange={setDateRange} />

      <MetricsGrid>
        <MetricCard
          title="Бронирований"
          value={data.totalBookings}
          change={data.bookingsChange}
          icon={<Calendar />}
        />
        <MetricCard
          title="Заработок"
          value={formatCurrency(data.totalEarnings)}
          change={data.earningsChange}
          icon={<Wallet />}
        />
        {/* ... */}
      </MetricsGrid>

      <EarningsChart data={data.earningsTimeline} />

      <Grid columns={2}>
        <PopularTimesWidget data={data.popularTimes} />
        <ClientInsightsWidget data={data.clientInsights} />
      </Grid>
    </div>
  )
}
```

##### 1.1.2. EarningsChart.tsx

**Библиотека:** Recharts или Chart.js

**Требования:**
- Line chart с двумя линиями: "Валовый доход" и "Чистый доход (после комиссии платформы)"
- Tooltip при наведении показывает детали дня
- Легенда для переключения линий
- Responsive дизайн
- Анимация при появлении

**Данные:**
```typescript
interface EarningsTimelineData {
  date: string // ISO date
  grossEarnings: number
  netEarnings: number
  platformFee: number
  bookingsCount: number
}
```

##### 1.1.3. RatingsBreakdown.tsx

**Визуализация рейтингов:**
```
Средний рейтинг: 4.8 ⭐ (из 42 отзывов)

★★★★★  85% ████████████████████████████ 36 отзывов
★★★★☆  12% ████                          5 отзывов
★★★☆☆   3% █                             1 отзыв
★★☆☆☆   0%
★☆☆☆☆   0%
```

**Требования:**
- Горизонтальные progress bars для каждого рейтинга
- Показывать количество и процент
- Анимация появления progress bars
- Кликабельные bars (переход к списку отзывов с этим рейтингом)

##### 1.1.4. PopularTimes.tsx

**Две визуализации:**

1. **Популярные дни недели** (Bar chart)
```typescript
interface PopularDay {
  day: string // 'Понедельник', 'Вторник', ...
  bookingsCount: number
}
```

2. **Популярные часы** (Heatmap или Bar chart)
```typescript
interface PopularHour {
  hour: string // '10:00', '11:00', ...
  bookingsCount: number
}
```

**Требования:**
- Топ-3 дня и топ-5 часов
- Визуальное выделение лидеров
- Процент от общего количества бронирований

##### 1.1.5. ClientInsights.tsx

**Метрики:**
- Всего клиентов
- Повторных клиентов (count + %)
- Среднее количество сессий на клиента
- Топ-3 клиента по количеству сессий (с анонимизацией: "Клиент А", "Клиент Б")

**Требования:**
- Карточки с иконками
- Tooltip с пояснениями метрик
- Ссылка "Посмотреть всех клиентов" → переход на `/dashboard/clients`

---

### 1.2. Platform Analytics (Admin Only)

**Route:** `/admin/analytics`

**API Endpoints:**
- `GET /api/analytics/platform/overview` - общие KPI
- `GET /api/analytics/platform/revenue` - финансовые метрики
- `GET /api/analytics/platform/growth-metrics` - рост пользователей
- `GET /api/analytics/platform/specialties-distribution` - распределение по специализациям
- `GET /api/analytics/platform/locations-distribution` - география
- `GET /api/analytics/platform/quality-metrics` - качество (no-show, cancellations)
- `GET /api/analytics/platform/time-slot-analysis` - анализ слотов

#### Компоненты

##### 1.2.1. PlatformOverview.tsx

**Макет:**
```
┌─────────────────────────────────────────────────────┐
│ Обзор платформы                                     │
│                                                     │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐│
│ │Пользовате│ │Специалист│ │ Клиенты  │ │Брониров.││
│ │ 1,248    │ │ 156      │ │ 892      │ │ 3,456   ││
│ │ +23 за 7д│ │ +5 за 7д │ │ +18 за 7д│ │ +124 ме││
│ └──────────┘ └──────────┘ └──────────┘ └─────────┘│
│                                                     │
│ ┌─────────────────────────────────────────────────┐│
│ │ 💰 Выручка платформы (Area Chart)              ││
│ │                                                 ││
│ │     ████████████████████████                    ││
│ │    ██████████████████████████                   ││
│ │   ████████████████████████████                  ││
│ └─────────────────────────────────────────────────┘│
│                                                     │
│ ┌────────────────────┐ ┌────────────────────────┐ │
│ │ 📍 География       │ │ 🎯 Специализации      │ │
│ │                    │ │                        │ │
│ │ Москва - 45%       │ │ Психолог - 40%        │ │
│ │ СПб - 28%          │ │ Коуч - 35%            │ │
│ │ Казань - 12%       │ │ Арт-терапевт - 15%    │ │
│ └────────────────────┘ └────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**Требования:**
- Глобальные метрики платформы
- Графики выручки, регистраций, бронирований
- Pie charts для распределений
- Real-time обновление (опционально, через WebSocket)
- Экспорт в CSV/Excel

##### 1.2.2. GrowthMetrics.tsx

**Графики роста:**
- Новые пользователи по дням/неделям/месяцам
- Новые специалисты
- Retention rate клиентов
- Churn rate

**Требования:**
- Multi-line charts
- Сравнение периодов (текущий vs предыдущий)
- Прогноз на следующий период (линейная регрессия)

##### 1.2.3. QualityMetrics.tsx

**Метрики качества:**
- No-show rate (% не пришедших клиентов)
- Cancellation rate (% отмененных бронирований)
- Average rating (средний рейтинг платформы)
- Specialist approval rate (% одобренных заявок специалистов)

**Требования:**
- Gauge charts для процентных метрик
- Color coding (зеленый = хорошо, красный = плохо)
- Тренды (улучшение/ухудшение)

---

## Модуль 2: Admin Dashboard

### 2.1. Users Management

**Route:** `/admin/users`

**API Endpoints:**
- `GET /api/admin/users` - список пользователей с фильтрами
- `GET /api/admin/users/:id` - детали пользователя
- `PUT /api/admin/users/:id/ban` - заблокировать
- `PUT /api/admin/users/:id/unban` - разблокировать

#### 2.1.1. UsersTable.tsx

**Макет:**
```
┌──────────────────────────────────────────────────────────────┐
│ Управление пользователями                                    │
│                                                              │
│ ┌─────────┐ ┌─────────┐ ┌─────────────────────────────────┐│
│ │Фильтр   │ │Роль     │ │ Поиск по email/имени            ││
│ │Все      │ │Все      │ │ 🔍                              ││
│ └─────────┘ └─────────┘ └─────────────────────────────────┘│
│                                                              │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │Email           │Имя      │Роль       │Статус  │Действия│ │
│ ├─────────────────────────────────────────────────────────┤ │
│ │ivan@mail.ru    │Иван С.  │CLIENT     │ACTIVE  │🔧 ⛔  │ │
│ │anna@mail.ru    │Анна П.  │SPECIALIST │ACTIVE  │🔧 ⛔  │ │
│ │admin@hearty.pro│Admin    │ADMIN      │ACTIVE  │🔧     │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                              │
│ Показано 1-20 из 1,248           [1] 2 3 ... 63            │
└──────────────────────────────────────────────────────────────┘
```

**Требования:**
- Server-side пагинация (20 записей на страницу)
- Фильтры: role (CLIENT/SPECIALIST/ADMIN), status (ACTIVE/SUSPENDED)
- Поиск по email, firstName, lastName (с debounce 300ms)
- Сортировка по столбцам
- Действия:
  - 🔧 "Просмотр деталей" → `/admin/users/:id`
  - ⛔ "Забанить" (с модальным окном для указания причины)
  - ✅ "Разбанить"
- Badges для ролей и статусов (цветовое кодирование)
- Skeleton loaders при загрузке

**Функционал:**
```typescript
const UsersTable = () => {
  const [filters, setFilters] = useState({ role: '', status: '', search: '' })
  const [page, setPage] = useState(1)
  const { data, loading } = useAdmin.getUsers({ ...filters, page, limit: 20 })

  const handleBan = async (userId: string, reason: string) => {
    await useAdmin.banUser(userId, { banReason: reason })
    // Refresh data
  }

  return (
    <div>
      <Filters onChange={setFilters} />
      <Table
        columns={columns}
        data={data.users}
        loading={loading}
        onRowClick={(user) => navigate(`/admin/users/${user.id}`)}
      />
      <Pagination
        current={page}
        total={data.pagination.totalPages}
        onChange={setPage}
      />
    </div>
  )
}
```

#### 2.1.2. UserDetails.tsx

**Route:** `/admin/users/:id`

**Макет:**
```
┌──────────────────────────────────────────────────────────────┐
│ ← Назад к пользователям                                      │
│                                                              │
│ 👤 Иван Сидоров                                              │
│                                                              │
│ ┌────────────────────┐ ┌────────────────────────────────┐   │
│ │ Основная информация│ │ Статистика активности          │   │
│ │                    │ │                                │   │
│ │ Email: ivan@...    │ │ Бронирований: 12               │   │
│ │ Роль: CLIENT       │ │ Сессий: 8                      │   │
│ │ Статус: ACTIVE     │ │ Отзывов: 3                     │   │
│ │ Регистрация:       │ │ Последний вход: 2 часа назад   │   │
│ │   15.01.2025       │ │                                │   │
│ └────────────────────┘ └────────────────────────────────┘   │
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐│
│ │ История активности                                       ││
│ │                                                          ││
│ │ 13.02.2025 - Создано бронирование #1234                 ││
│ │ 10.02.2025 - Завершена сессия #567                      ││
│ │ 05.02.2025 - Оставлен отзыв на Анна Петрова (5⭐)       ││
│ └──────────────────────────────────────────────────────────┘│
│                                                              │
│ 🚫 Забанить пользователя   ✅ Разбанить                     │
└──────────────────────────────────────────────────────────────┘
```

**Требования:**
- Полная информация о пользователе
- Связанные данные (specialist/client profile если есть)
- История активности (последние 50 событий)
- Кнопки действий:
  - "Забанить" → модальное окно с полем причины
  - "Разбанить" (если уже забанен)
  - "Сбросить пароль" (опционально)
- Breadcrumbs для навигации

---

### 2.2. Specialists Moderation

**Route:** `/admin/specialists`

**API Endpoints:**
- `GET /api/admin/specialists` - список для модерации
- `PUT /api/admin/specialists/:id/approve` - одобрить
- `PUT /api/admin/specialists/:id/reject` - отклонить
- `PUT /api/admin/specialists/:id/suspend` - приостановить

#### 2.2.1. SpecialistsModeration.tsx

**Макет:**
```
┌──────────────────────────────────────────────────────────────┐
│ Модерация специалистов                                       │
│                                                              │
│ ┌──────────────┐                                            │
│ │Статус        │                                            │
│ │PENDING       │  (фильтр: PENDING, APPROVED, REJECTED, ... │
│ └──────────────┘                                            │
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐│
│ │ 📋 Заявка #1: Мария Иванова                              ││
│ │                                                          ││
│ │ Специализация: Семейный психолог                        ││
│ │ Опыт: 8 лет                                              ││
│ │ Образование: МГУ, семейная психология                   ││
│ │ Сертификаты: Сертификат семейного психолога             ││
│ │                                                          ││
│ │ ✅ Одобрить    ❌ Отклонить    ⏸️ Приостановить        ││
│ └──────────────────────────────────────────────────────────┘│
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐│
│ │ 📋 Заявка #2: Алексей Волков                             ││
│ │ ...                                                      ││
│ └──────────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────┘
```

**Требования:**
- Фильтр по статусу (PENDING, APPROVED, REJECTED, SUSPENDED)
- Карточки с подробной информацией о специалисте
- Действия:
  - "Одобрить" → instant approval
  - "Отклонить" → модальное окно с полем причины (moderationNote)
  - "Приостановить" → модальное окно с причиной
- Badge для статуса (цвета: PENDING=желтый, APPROVED=зеленый, REJECTED=красный)
- Показывать количество бронирований/отзывов/сессий для APPROVED специалистов

---

### 2.3. Reviews Moderation

**Route:** `/admin/reviews`

**API Endpoints:**
- `GET /api/admin/reviews` - список отзывов
- `PUT /api/admin/reviews/:id/approve` - одобрить
- `PUT /api/admin/reviews/:id/reject` - отклонить

#### 2.3.1. ReviewsModeration.tsx

**Макет:**
```
┌──────────────────────────────────────────────────────────────┐
│ Модерация отзывов                                            │
│                                                              │
│ ┌─────────────┐                                             │
│ │Статус       │                                             │
│ │PENDING      │                                             │
│ └─────────────┘                                             │
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐│
│ │ ⭐⭐⭐⭐⭐ Отзыв от Иван Сидоров                         ││
│ │ На специалиста: Анна Петрова                             ││
│ │                                                          ││
│ │ "Отличный специалист! Помог разобраться в сложной       ││
│ │  ситуации. Рекомендую!"                                  ││
│ │                                                          ││
│ │ Дата: 13.02.2025                                         ││
│ │                                                          ││
│ │ ✅ Одобрить    ❌ Отклонить                              ││
│ └──────────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────┘
```

**Требования:**
- Фильтр по статусу (PENDING, APPROVED, REJECTED)
- Показывать рейтинг (звездочки), текст отзыва, автора, специалиста
- Действия:
  - "Одобрить" → обновляет рейтинг специалиста
  - "Отклонить" → модальное окно с причиной
- Highlight оскорбительных слов (опционально, ML модель)

---

### 2.4. Bookings Management

**Route:** `/admin/bookings`

**API Endpoints:**
- `GET /api/admin/bookings` - все бронирования
- `PUT /api/admin/bookings/:id/cancel` - отменить с возвратом

#### 2.4.1. BookingsTable.tsx

**Макет:**
```
┌──────────────────────────────────────────────────────────────┐
│ Управление бронированиями                                    │
│                                                              │
│ ┌─────────┐ ┌────────────┐ ┌────────────┐                  │
│ │Статус   │ │Специалист  │ │Клиент      │                  │
│ │Все      │ │Все         │ │Все         │                  │
│ └─────────┘ └────────────┘ └────────────┘                  │
│                                                              │
│ Период: [от 01.01.2025] [до 13.02.2025]                     │
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐│
│ │ID    │Дата  │Время │Клиент │Специалист│Цена │Статус   │ ││
│ ├──────────────────────────────────────────────────────────┤│
│ │#1234 │13.02 │18:00 │Иван С.│Анна П.   │4000₽│COMPLETED│ ││
│ │#1235 │14.02 │10:00 │Мария К│Дмитрий С.│5000₽│PENDING  │ ││
│ └──────────────────────────────────────────────────────────┘│
│                                                              │
│ Действия: 🔧 Детали  ❌ Отменить с возвратом                │
└──────────────────────────────────────────────────────────────┘
```

**Требования:**
- Фильтры: status, specialistId, clientId, dateFrom, dateTo
- Сортировка по дате, цене, статусу
- Действия:
  - "Детали" → модальное окно с полной информацией
  - "Отменить с возвратом" → модальное окно с полем причины
- Статусы с цветами: PENDING=желтый, CONFIRMED=синий, COMPLETED=зеленый, CANCELLED=красный
- Экспорт в Excel/CSV

---

### 2.5. Financial Reports

**Route:** `/admin/financials`

**API Endpoints:**
- `GET /api/admin/financials/summary` - общий отчет
- `GET /api/admin/financials/specialists/:id` - отчет по специалисту

#### 2.5.1. FinancialReports.tsx

**Макет:**
```
┌──────────────────────────────────────────────────────────────┐
│ Финансовые отчеты                                            │
│                                                              │
│ Период: [от 01.01.2025] [до 13.02.2025]                     │
│                                                              │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐         │
│ │Выручка   │ │Комиссия  │ │Выплачено │ │Возвраты │         │
│ │1,250,000₽│ │187,500₽  │ │850,000₽  │ │35,000₽  │         │
│ │          │ │(15%)     │ │          │ │         │         │
│ └──────────┘ └──────────┘ └──────────┘ └─────────┘         │
│                                                              │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 💰 Выручка по дням (Area Chart)                         │ │
│ │                                                         │ │
│ │     ████████████████████████                            │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                              │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Выплаты специалистам                                    │ │
│ │                                                         │ │
│ │ Анна Петрова - 96,000₽ (24 сессии)                     │ │
│ │ Дмитрий Соколов - 120,000₽ (24 сессии)                 │ │
│ │ ...                                                     │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                              │
│ 📥 Экспорт в Excel   📄 Скачать PDF                         │
└──────────────────────────────────────────────────────────────┘
```

**Требования:**
- Выбор периода (preset + custom range)
- Ключевые метрики: totalRevenue, totalPlatformFee, totalRefunds, netRevenue
- График выручки с группировкой (день/неделя/месяц)
- Таблица выплат специалистам (топ-20)
- Экспорт в Excel (xlsx), PDF
- Breakdown по типам транзакций (BOOKING_PAYMENT, REFUND, SPECIALIST_PAYOUT)

---

## Модуль 3: Real-time Features

### 3.1. WebSocket Integration

**Библиотека:** socket.io-client

#### 3.1.1. useWebSocket.ts Hook

**Функциональность:**
```typescript
import { useEffect, useState } from 'react'
import { io, Socket } from 'socket.io-client'

export const useWebSocket = () => {
  const [socket, setSocket] = useState<Socket | null>(null)
  const [connected, setConnected] = useState(false)

  useEffect(() => {
    const token = localStorage.getItem('authToken')
    if (!token) return

    const socketInstance = io(import.meta.env.VITE_WS_URL || 'https://heartypro-back-production.up.railway.app', {
      auth: { token },
      transports: ['websocket', 'polling']
    })

    socketInstance.on('connect', () => {
      console.log('✅ Connected to WebSocket')
      setConnected(true)
    })

    socketInstance.on('disconnect', () => {
      console.log('❌ Disconnected from WebSocket')
      setConnected(false)
    })

    socketInstance.on('error', (error) => {
      console.error('🔴 WebSocket error:', error)
    })

    setSocket(socketInstance)

    return () => {
      socketInstance.disconnect()
    }
  }, [])

  return { socket, connected }
}
```

---

### 3.2. Real-time Chat

**Route:** `/dashboard/chat` (для специалистов), `/client/chat` (для клиентов)

**WebSocket Events:**
- `chat:join` - подключиться к комнате
- `chat:leave` - покинуть комнату
- `chat:message:new` - новое сообщение (от сервера)
- `chat:typing` - индикатор набора
- `chat:message:read` - сообщение прочитано

#### 3.2.1. ChatPage.tsx

**Макет:**
```
┌──────────────────────────────────────────────────────────────┐
│ Сообщения                                                    │
│ ┌──────────────┐ ┌────────────────────────────────────────┐ │
│ │Список чатов  │ │ Чат с Анна Петрова                      │ │
│ │              │ │ ┌────────────────────────────────────┐ │ │
│ │ 🟢 Анна П.   │ │ │ Привет! Как дела?                  │ │ │
│ │   Печатает...│ │ │                         10:30  ← Я│ │ │
│ │              │ │ ├────────────────────────────────────┤ │ │
│ │ 🔴 Дмитрий С.│ │ │ Анна → Здравствуйте!              │ │ │
│ │   2 нов. сооб│ │ │        Готова помочь 😊  10:32    │ │ │
│ │              │ │ └────────────────────────────────────┘ │ │
│ │ Иван В.      │ │                                        │ │
│ │              │ │ ┌────────────────────────────────────┐ │ │
│ └──────────────┘ │ │ Введите сообщение...         [📎]│ │ │
│                  │ └────────────────────────────────────┘ │ │
│                  │ [Отправить]                            │ │
│                  └────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

**Требования:**
- Двухпанельный layout: список чатов (левая колонка) + активный чат (правая)
- Список чатов:
  - Показать аватар, имя, последнее сообщение, время
  - Badge с количеством непрочитанных
  - Онлайн статус (зеленая точка = онлайн, серая = оффлайн)
  - Индикатор "Печатает..." для активного собеседника
- Окно чата:
  - Автоскролл к последнему сообщению
  - Разделение сообщений по датам ("Сегодня", "Вчера", "12 февраля")
  - Визуальное отличие своих и чужих сообщений
  - Статус прочитанности (✓✓ = прочитано)
  - Timestamp для каждого сообщения
- Поле ввода:
  - Автофокус при открытии чата
  - Enter для отправки, Shift+Enter для новой строки
  - Кнопка прикрепления файлов (опционально)
  - Индикатор "Печатает..." отправляется при наборе текста (debounce 1s)

**Функционал:**
```typescript
const ChatPage = () => {
  const { socket, connected } = useWebSocket()
  const [activeRoomId, setActiveRoomId] = useState<string | null>(null)
  const [messages, setMessages] = useState<Message[]>([])
  const [inputText, setInputText] = useState('')
  const [isTyping, setIsTyping] = useState(false)

  useEffect(() => {
    if (!socket || !activeRoomId) return

    // Join room
    socket.emit('chat:join', { roomId: activeRoomId })

    // Listen for new messages
    socket.on('chat:message:new', (message: Message) => {
      setMessages(prev => [...prev, message])
    })

    // Listen for typing
    socket.on('chat:typing', ({ userId, isTyping }) => {
      setIsTyping(isTyping)
    })

    return () => {
      socket.emit('chat:leave', { roomId: activeRoomId })
    }
  }, [socket, activeRoomId])

  const sendMessage = async () => {
    // Send via REST API
    await fetch(`${API_URL}/chat/messages`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${token}` },
      body: JSON.stringify({ roomId: activeRoomId, text: inputText })
    })
    setInputText('')
  }

  const handleTyping = useDebounce(() => {
    socket?.emit('chat:typing', { roomId: activeRoomId, isTyping: true })
    setTimeout(() => {
      socket?.emit('chat:typing', { roomId: activeRoomId, isTyping: false })
    }, 1000)
  }, 500)

  return (
    <div className="chat-page">
      <ChatList onSelectRoom={setActiveRoomId} />
      <ChatWindow
        roomId={activeRoomId}
        messages={messages}
        isTyping={isTyping}
        inputText={inputText}
        onInputChange={(text) => {
          setInputText(text)
          handleTyping()
        }}
        onSendMessage={sendMessage}
      />
    </div>
  )
}
```

#### 3.2.2. MessageBubble.tsx

**Требования:**
- Разные стили для своих и чужих сообщений
- Timestamp в относительном формате ("5 минут назад", "Вчера в 18:30")
- Иконки статуса прочитанности
- Поддержка markdown (жирный, курсив, ссылки) - опционально
- Emoji picker - опционально

---

### 3.3. Real-time Notifications

**Route:** Глобальный компонент (в Layout)

**WebSocket Events:**
- `notification:new` - новое уведомление

#### 3.3.1. NotificationBell.tsx

**Макет:**
```
┌────────────────────────────────┐
│ 🔔 (5)                         │  ← Badge с количеством
│                                │
│ ┌────────────────────────────┐│
│ │ Уведомления                ││
│ ├────────────────────────────┤│
│ │ 📅 Новое бронирование      ││
│ │    Иван С. забронировал... ││
│ │    2 минуты назад          ││
│ ├────────────────────────────┤│
│ │ ⭐ Новый отзыв             ││
│ │    Мария К. оставила отзыв ││
│ │    1 час назад             ││
│ ├────────────────────────────┤│
│ │ 💬 Новое сообщение         ││
│ │    Анна П.: Привет!        ││
│ │    3 часа назад            ││
│ └────────────────────────────┘│
│                                │
│ [Отметить все прочитанными]    │
│ [Посмотреть все]               │
└────────────────────────────────┘
```

**Требования:**
- Иконка колокольчика в header с badge (количество непрочитанных)
- Dropdown при клике на колокольчик
- Типы уведомлений:
  - NEW_BOOKING (для специалистов)
  - BOOKING_CONFIRMED (для клиентов)
  - NEW_MESSAGE (для обоих)
  - NEW_REVIEW (для специалистов)
  - PAYMENT_RECEIVED (для специалистов)
  - WITHDRAWAL_PROCESSED (для специалистов)
- Кликабельные уведомления (переход к связанному объекту)
- Sound/Desktop notification при новом уведомлении (опционально, с разрешением пользователя)
- Auto-refresh каждые 30 секунд через REST API (fallback если WebSocket не работает)

**Функционал:**
```typescript
const NotificationBell = () => {
  const { socket } = useWebSocket()
  const [notifications, setNotifications] = useState<Notification[]>([])
  const [unreadCount, setUnreadCount] = useState(0)
  const [isOpen, setIsOpen] = useState(false)

  useEffect(() => {
    if (!socket) return

    socket.on('notification:new', (notification: Notification) => {
      setNotifications(prev => [notification, ...prev])
      setUnreadCount(prev => prev + 1)

      // Play sound
      playNotificationSound()

      // Show desktop notification
      if (Notification.permission === 'granted') {
        new Notification(notification.title, {
          body: notification.message,
          icon: '/logo.png'
        })
      }
    })
  }, [socket])

  const markAllAsRead = async () => {
    await fetch(`${API_URL}/notifications/mark-all-read`, {
      method: 'POST',
      headers: { 'Authorization': `Bearer ${token}` }
    })
    setUnreadCount(0)
  }

  return (
    <div className="notification-bell">
      <button onClick={() => setIsOpen(!isOpen)}>
        🔔 {unreadCount > 0 && <Badge>{unreadCount}</Badge>}
      </button>

      {isOpen && (
        <Dropdown>
          <NotificationList notifications={notifications.slice(0, 10)} />
          <button onClick={markAllAsRead}>Отметить все прочитанными</button>
          <Link to="/notifications">Посмотреть все</Link>
        </Dropdown>
      )}
    </div>
  )
}
```

---

### 3.4. Presence (Online Status)

**WebSocket Events:**
- `user:online` - пользователь онлайн
- `user:offline` - пользователь оффлайн

#### 3.4.1. OnlineIndicator.tsx

**Требования:**
- Зеленая точка рядом с аватаром если пользователь онлайн
- Серая точка если оффлайн
- Tooltip с временем последней активности ("Онлайн", "Был в сети 5 минут назад")
- Использовать в:
  - Списке чатов
  - Профиле специалиста
  - Списке специалистов (опционально)

**Функционал:**
```typescript
const OnlineIndicator = ({ userId }: { userId: string }) => {
  const { socket } = useWebSocket()
  const [isOnline, setIsOnline] = useState(false)

  useEffect(() => {
    if (!socket) return

    socket.on('user:online', ({ userId: onlineUserId }) => {
      if (onlineUserId === userId) {
        setIsOnline(true)
      }
    })

    socket.on('user:offline', ({ userId: offlineUserId }) => {
      if (offlineUserId === userId) {
        setIsOnline(false)
      }
    })
  }, [socket, userId])

  return (
    <div className={`online-indicator ${isOnline ? 'online' : 'offline'}`}>
      <div className="dot"></div>
    </div>
  )
}
```

---

## Модуль 4: UI/UX Improvements

### 4.1. Переиспользуемые компоненты

#### 4.1.1. Card.tsx

**Использование:** Все карточки на сайте (метрики, специалисты, отзывы)

**Props:**
```typescript
interface CardProps {
  children: React.ReactNode
  variant?: 'default' | 'outlined' | 'elevated'
  padding?: 'sm' | 'md' | 'lg'
  onClick?: () => void
  className?: string
}
```

**Требования:**
- Скругленные углы (radius из index.css)
- Shadow для variant='elevated'
- Hover эффект для кликабельных карточек
- Responsive padding

#### 4.1.2. Table.tsx

**Использование:** Все таблицы в админке

**Props:**
```typescript
interface TableProps<T> {
  columns: Column<T>[]
  data: T[]
  loading?: boolean
  onRowClick?: (row: T) => void
  sortable?: boolean
}

interface Column<T> {
  key: keyof T
  title: string
  render?: (value: any, row: T) => React.ReactNode
  sortable?: boolean
  width?: string
}
```

**Требования:**
- Сортировка по столбцам (стрелки вверх/вниз)
- Skeleton loaders при loading=true
- Hover эффект на строках
- Sticky header при скролле
- Responsive (горизонтальный scroll на мобильных)

#### 4.1.3. Chart.tsx

**Библиотека:** Recharts

**Типы графиков:**
- LineChart (для earnings timeline, growth metrics)
- AreaChart (для revenue)
- BarChart (для popular times)
- PieChart (для distributions)

**Props:**
```typescript
interface ChartProps {
  type: 'line' | 'area' | 'bar' | 'pie'
  data: any[]
  xKey: string
  yKey: string | string[]
  colors?: string[]
  height?: number
  loading?: boolean
}
```

**Требования:**
- Responsive (адаптируется к ширине контейнера)
- Tooltip с красивым дизайном
- Legend
- Анимация при появлении
- Skeleton loader при loading=true
- Экспорт графика в PNG (опционально)

#### 4.1.4. DateRangePicker.tsx

**Библиотека:** react-day-picker или react-datepicker

**Функциональность:**
- Preset ranges: "Последние 7 дней", "30 дней", "90 дней", "Год", "Кастомный период"
- Календарь для выбора произвольного периода
- Validation (to >= from)
- Локализация на русский язык

**Props:**
```typescript
interface DateRangePickerProps {
  value: { from: Date; to: Date }
  onChange: (range: { from: Date; to: Date }) => void
  presets?: string[]
}
```

#### 4.1.5. Badge.tsx

**Использование:** Статусы, роли, категории

**Props:**
```typescript
interface BadgeProps {
  children: React.ReactNode
  variant: 'success' | 'warning' | 'error' | 'info' | 'default'
  size?: 'sm' | 'md' | 'lg'
}
```

**Цветовая схема:**
- success: зеленый (для APPROVED, ACTIVE, COMPLETED)
- warning: желтый (для PENDING, IN_PROGRESS)
- error: красный (для REJECTED, CANCELLED, SUSPENDED)
- info: синий (для INFO типов)
- default: серый

#### 4.1.6. Tabs.tsx

**Использование:** Навигация внутри страниц (например, Analytics: "Обзор", "Заработок", "Клиенты")

**Props:**
```typescript
interface TabsProps {
  tabs: Tab[]
  activeTab: string
  onChange: (tabId: string) => void
}

interface Tab {
  id: string
  label: string
  icon?: React.ReactNode
  badge?: number
}
```

**Требования:**
- Underline активного таба
- Smooth transition при переключении
- Иконки слева от label (опционально)
- Badge справа от label (например, "Уведомления (5)")

---

### 4.2. Общие улучшения UX

#### 4.2.1. Loading States

**Skeleton Loaders:**
- Использовать для всех компонентов при загрузке данных
- Повторять структуру реального контента
- Пульсирующая анимация

**Примеры:**
```typescript
// Skeleton для карточки специалиста
<SkeletonCard>
  <SkeletonAvatar />
  <SkeletonText lines={2} />
  <SkeletonButton />
</SkeletonCard>

// Skeleton для таблицы
<SkeletonTable rows={10} columns={5} />

// Skeleton для графика
<SkeletonChart height={300} />
```

#### 4.2.2. Error States

**Требования:**
- Friendly error messages (без технических деталей)
- Кнопка "Попробовать снова"
- Иллюстрация или иконка ошибки
- Цветовое кодирование (красная рамка, иконка)

**Примеры:**
```typescript
<ErrorState
  title="Не удалось загрузить данные"
  message="Проверьте подключение к интернету и попробуйте снова"
  onRetry={() => refetch()}
/>
```

#### 4.2.3. Empty States

**Требования:**
- Понятное сообщение о том, почему нет данных
- CTA для создания первой записи (если применимо)
- Иллюстрация

**Примеры:**
```typescript
// Пустой список уведомлений
<EmptyState
  icon={<Bell />}
  title="Нет уведомлений"
  message="Здесь будут появляться уведомления о новых бронированиях, сообщениях и отзывах"
/>

// Пустой список бронирований
<EmptyState
  icon={<Calendar />}
  title="У вас пока нет бронирований"
  message="Клиенты смогут бронировать сессии после одобрения вашей заявки"
  action={<Button onClick={goToOnboarding}>Заполнить профиль</Button>}
/>
```

#### 4.2.4. Toasts (Notifications)

**Библиотека:** react-hot-toast или sonner

**Типы:**
- success: "Изменения сохранены ✅"
- error: "Ошибка при сохранении ❌"
- info: "Бронирование подтверждено ℹ️"
- loading: "Загрузка... ⏳"

**Требования:**
- Автоматическое скрытие через 3-5 секунд
- Кнопка закрытия
- Позиция: top-right
- Анимация появления/исчезновения

#### 4.2.5. Modals

**Типы:**
- Confirmation modal (для удаления, блокировки)
- Form modal (для создания/редактирования)
- Info modal (для просмотра деталей)

**Требования:**
- Backdrop с затемнением
- Закрытие по клику на backdrop или Esc
- Анимация появления
- Фокус на первое поле ввода (для form modals)
- Кнопки "Отмена" и "Подтвердить" (для confirmation)

**Пример:**
```typescript
<Modal open={isOpen} onClose={() => setIsOpen(false)}>
  <ModalTitle>Заблокировать пользователя?</ModalTitle>
  <ModalBody>
    <Input
      label="Причина блокировки"
      value={reason}
      onChange={setReason}
      required
    />
  </ModalBody>
  <ModalFooter>
    <Button variant="outline" onClick={() => setIsOpen(false)}>Отмена</Button>
    <Button variant="destructive" onClick={handleBan}>Заблокировать</Button>
  </ModalFooter>
</Modal>
```

---

## Технический стек

### Библиотеки и инструменты

**Уже используется:**
- ✅ React 18
- ✅ TypeScript
- ✅ Vite
- ✅ React Router v6
- ✅ Tailwind CSS
- ✅ Lucide React (иконки)

**Нужно добавить:**

```json
{
  "dependencies": {
    "socket.io-client": "^4.8.0",           // WebSocket
    "recharts": "^2.12.0",                  // Графики
    "react-day-picker": "^9.6.0",           // DatePicker
    "date-fns": "^4.1.0",                   // Форматирование дат
    "react-hot-toast": "^2.5.0",            // Toasts
    "zustand": "^5.2.0",                    // State management (опционально)
    "@tanstack/react-query": "^5.62.0",     // Data fetching (опционально)
    "axios": "^1.7.9",                      // HTTP client
    "zod": "^4.3.6"                         // Валидация
  },
  "devDependencies": {
    "@types/node": "^25.2.3"
  }
}
```

### Архитектурные паттерны

#### State Management

**Опция 1: React Context + Hooks (текущий подход)**
- Плюсы: Нет дополнительных зависимостей
- Минусы: Может быть сложно при большом количестве состояния

**Опция 2: Zustand (рекомендуется)**
- Простой и легковесный
- Хорошо подходит для глобального состояния (auth, notifications, WebSocket)

```typescript
// stores/auth.ts
import create from 'zustand'

interface AuthStore {
  user: User | null
  token: string | null
  login: (email: string, password: string) => Promise<void>
  logout: () => void
}

export const useAuthStore = create<AuthStore>((set) => ({
  user: null,
  token: localStorage.getItem('authToken'),
  login: async (email, password) => {
    const { user, token } = await api.login(email, password)
    localStorage.setItem('authToken', token)
    set({ user, token })
  },
  logout: () => {
    localStorage.removeItem('authToken')
    set({ user: null, token: null })
  }
}))
```

#### Data Fetching

**Опция 1: useEffect + useState (текущий подход)**
- Работает, но много boilerplate кода

**Опция 2: React Query (рекомендуется для Analytics + Admin)**
- Автоматический кэш
- Retry логика
- Refetch при focus окна
- Optimistic updates

```typescript
// hooks/useAnalytics.ts
import { useQuery } from '@tanstack/react-query'

export const useSpecialistDashboard = (specialistId: string, dateRange: DateRange) => {
  return useQuery({
    queryKey: ['analytics', 'specialist', specialistId, dateRange],
    queryFn: () => api.analytics.getSpecialistDashboard(specialistId, dateRange),
    staleTime: 5 * 60 * 1000, // 5 minutes
    retry: 2
  })
}
```

---

## API Integration

### API Client

**services/api.ts:**
```typescript
import axios from 'axios'

const API_URL = import.meta.env.VITE_API_URL || 'https://heartypro-back-production.up.railway.app/api'

const apiClient = axios.create({
  baseURL: API_URL,
  headers: {
    'Content-Type': 'application/json'
  }
})

// Request interceptor (добавляем JWT токен)
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('authToken')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// Response interceptor (обработка ошибок)
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      // Redirect to login
      localStorage.removeItem('authToken')
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)

export default apiClient
```

### Analytics API Client

**services/analytics.ts:**
```typescript
import apiClient from './api'
import { DateRangeFilter } from '../types/analytics'

export const analyticsApi = {
  // Specialist analytics
  getSpecialistDashboard: (id: string, dateRange?: DateRangeFilter) =>
    apiClient.get(`/analytics/specialist/${id}/dashboard`, { params: dateRange }).then(res => res.data),

  getSpecialistEarnings: (id: string, dateRange?: DateRangeFilter) =>
    apiClient.get(`/analytics/specialist/${id}/earnings`, { params: dateRange }).then(res => res.data),

  getSpecialistBookingsTimeline: (id: string, dateRange?: DateRangeFilter) =>
    apiClient.get(`/analytics/specialist/${id}/bookings-timeline`, { params: dateRange }).then(res => res.data),

  getSpecialistRatingsBreakdown: (id: string) =>
    apiClient.get(`/analytics/specialist/${id}/ratings-breakdown`).then(res => res.data),

  getSpecialistPopularTimes: (id: string) =>
    apiClient.get(`/analytics/specialist/${id}/popular-times`).then(res => res.data),

  getSpecialistClientInsights: (id: string) =>
    apiClient.get(`/analytics/specialist/${id}/client-insights`).then(res => res.data),

  // Platform analytics (admin only)
  getPlatformOverview: () =>
    apiClient.get('/analytics/platform/overview').then(res => res.data),

  getPlatformRevenue: (dateRange?: DateRangeFilter) =>
    apiClient.get('/analytics/platform/revenue', { params: dateRange }).then(res => res.data),

  getPlatformGrowthMetrics: (dateRange?: DateRangeFilter) =>
    apiClient.get('/analytics/platform/growth-metrics', { params: dateRange }).then(res => res.data),

  getSpecialtiesDistribution: () =>
    apiClient.get('/analytics/platform/specialties-distribution').then(res => res.data),

  getLocationsDistribution: () =>
    apiClient.get('/analytics/platform/locations-distribution').then(res => res.data),

  getQualityMetrics: () =>
    apiClient.get('/analytics/platform/quality-metrics').then(res => res.data),

  getTimeSlotAnalysis: () =>
    apiClient.get('/analytics/platform/time-slot-analysis').then(res => res.data)
}
```

### Admin API Client

**services/admin.ts:**
```typescript
import apiClient from './api'

export const adminApi = {
  // Users
  getUsers: (params: { page: number; limit: number; role?: string; status?: string; search?: string }) =>
    apiClient.get('/admin/users', { params }).then(res => res.data),

  getUserDetails: (id: string) =>
    apiClient.get(`/admin/users/${id}`).then(res => res.data),

  banUser: (id: string, data: { banReason: string }) =>
    apiClient.put(`/admin/users/${id}/ban`, data).then(res => res.data),

  unbanUser: (id: string) =>
    apiClient.put(`/admin/users/${id}/unban`).then(res => res.data),

  // Specialists moderation
  getSpecialists: (params: { page: number; limit: number; status?: string }) =>
    apiClient.get('/admin/specialists', { params }).then(res => res.data),

  approveSpecialist: (id: string) =>
    apiClient.put(`/admin/specialists/${id}/approve`).then(res => res.data),

  rejectSpecialist: (id: string, data: { moderationNote: string }) =>
    apiClient.put(`/admin/specialists/${id}/reject`, data).then(res => res.data),

  suspendSpecialist: (id: string, data: { moderationNote: string }) =>
    apiClient.put(`/admin/specialists/${id}/suspend`, data).then(res => res.data),

  // Reviews moderation
  getReviews: (params: { page: number; limit: number; status?: string }) =>
    apiClient.get('/admin/reviews', { params }).then(res => res.data),

  approveReview: (id: string) =>
    apiClient.put(`/admin/reviews/${id}/approve`).then(res => res.data),

  rejectReview: (id: string, data: { moderationNote: string }) =>
    apiClient.put(`/admin/reviews/${id}/reject`, data).then(res => res.data),

  // Bookings
  getBookings: (params: any) =>
    apiClient.get('/admin/bookings', { params }).then(res => res.data),

  cancelBooking: (id: string, data: { reason: string }) =>
    apiClient.put(`/admin/bookings/${id}/cancel`, data).then(res => res.data),

  // Financials
  getFinancialSummary: (params: { dateFrom: string; dateTo: string }) =>
    apiClient.get('/admin/financials/summary', { params }).then(res => res.data),

  getSpecialistFinancials: (id: string, params?: { dateFrom?: string; dateTo?: string }) =>
    apiClient.get(`/admin/financials/specialists/${id}`, { params }).then(res => res.data),

  // Stats
  getPlatformStats: () =>
    apiClient.get('/admin/stats').then(res => res.data),

  // Transactions
  getTransactions: (params: { page: number; limit: number; status?: string; type?: string }) =>
    apiClient.get('/admin/transactions', { params }).then(res => res.data)
}
```

### WebSocket Client

**services/socket.ts:**
```typescript
import { io, Socket } from 'socket.io-client'

const WS_URL = import.meta.env.VITE_WS_URL || 'https://heartypro-back-production.up.railway.app'

let socket: Socket | null = null

export const initializeSocket = (token: string) => {
  if (socket) {
    socket.disconnect()
  }

  socket = io(WS_URL, {
    auth: { token },
    transports: ['websocket', 'polling'],
    reconnection: true,
    reconnectionDelay: 1000,
    reconnectionAttempts: 5
  })

  socket.on('connect', () => {
    console.log('✅ Connected to WebSocket')
  })

  socket.on('disconnect', (reason) => {
    console.log('❌ Disconnected from WebSocket:', reason)
  })

  socket.on('error', (error) => {
    console.error('🔴 WebSocket error:', error)
  })

  return socket
}

export const getSocket = () => {
  if (!socket) {
    throw new Error('Socket not initialized. Call initializeSocket first.')
  }
  return socket
}

export const disconnectSocket = () => {
  if (socket) {
    socket.disconnect()
    socket = null
  }
}
```

---

## Приоритеты и сроки

### Фаза 1: Analytics Dashboard (Неделя 1-2)

**Приоритет:** HIGH
**Сложность:** Medium
**Время:** 7-10 дней

**Deliverables:**
- ✅ SpecialistDashboard.tsx
- ✅ EarningsChart.tsx
- ✅ BookingsTimeline.tsx
- ✅ RatingsBreakdown.tsx
- ✅ PopularTimes.tsx
- ✅ ClientInsights.tsx
- ✅ PlatformOverview.tsx (admin)
- ✅ GrowthMetrics.tsx (admin)
- ✅ QualityMetrics.tsx (admin)
- ✅ Интеграция с Analytics API
- ✅ Routes: `/dashboard/analytics`, `/admin/analytics`

**Acceptance Criteria:**
- Специалист может видеть свою статистику за разные периоды
- Админ может видеть платформенные метрики
- Графики корректно отображаются и анимируются
- Фильтр по периоду работает
- Экспорт в PDF работает (опционально)
- Responsive дизайн на мобильных

---

### Фаза 2: Admin Dashboard (Неделя 2-3)

**Приоритет:** HIGH
**Сложность:** Medium-High
**Время:** 7-12 дней

**Deliverables:**
- ✅ UsersTable.tsx
- ✅ UserDetails.tsx
- ✅ SpecialistsModeration.tsx
- ✅ ReviewsModeration.tsx
- ✅ BookingsTable.tsx
- ✅ FinancialReports.tsx
- ✅ Интеграция с Admin API
- ✅ Routes: `/admin/users`, `/admin/specialists`, `/admin/reviews`, `/admin/bookings`, `/admin/financials`

**Acceptance Criteria:**
- Админ может просматривать всех пользователей с фильтрами
- Админ может банить/разбанивать пользователей
- Админ может модерировать специалистов (approve/reject/suspend)
- Админ может модерировать отзывы (approve/reject)
- Админ может просматривать и отменять бронирования
- Админ может генерировать финансовые отчеты
- Все таблицы работают с пагинацией и сортировкой
- Модальные окна для подтверждения действий работают

---

### Фаза 3: Real-time Features (Неделя 3-4)

**Приоритет:** HIGH (конкурентное преимущество)
**Сложность:** High
**Время:** 10-14 дней

**Deliverables:**
- ✅ useWebSocket.ts hook
- ✅ ChatPage.tsx
- ✅ ChatWindow.tsx
- ✅ ChatList.tsx
- ✅ MessageBubble.tsx
- ✅ TypingIndicator.tsx
- ✅ NotificationBell.tsx
- ✅ NotificationList.tsx
- ✅ OnlineIndicator.tsx
- ✅ Интеграция с Socket.IO
- ✅ Routes: `/dashboard/chat`, `/client/chat`

**Acceptance Criteria:**
- WebSocket подключение работает стабильно
- Чат отображает сообщения в реальном времени
- Индикатор "Печатает..." работает
- Уведомления приходят в реальном времени
- Онлайн-статус обновляется корректно
- Reconnection работает при потере соединения
- Desktop notifications работают (с разрешением пользователя)
- Sound notifications работают (опционально)

---

### Фаза 4: UI/UX Polish (Неделя 4-5)

**Приоритет:** MEDIUM
**Сложность:** Low-Medium
**Время:** 5-7 дней

**Deliverables:**
- ✅ Card.tsx
- ✅ Table.tsx
- ✅ Chart.tsx
- ✅ Badge.tsx
- ✅ Tabs.tsx
- ✅ DateRangePicker.tsx
- ✅ Modal.tsx
- ✅ EmptyState.tsx
- ✅ ErrorState.tsx
- ✅ SkeletonLoaders
- ✅ Toasts integration
- ✅ Responsive design для всех страниц

**Acceptance Criteria:**
- Все компоненты переиспользуются в проекте
- Skeleton loaders показываются при загрузке
- Error states показываются при ошибках
- Empty states показываются когда нет данных
- Toasts появляются при успешных/неудачных действиях
- Все страницы адаптивны на мобильных
- Анимации плавные и не тормозят

---

## Финальный чеклист готовности

### Analytics Dashboard ✓
- [ ] Specialist dashboard работает с фильтром по периоду
- [ ] Earnings chart отображается корректно
- [ ] Bookings timeline работает
- [ ] Ratings breakdown визуализируется
- [ ] Popular times показываются
- [ ] Client insights доступны
- [ ] Platform overview (admin) работает
- [ ] Growth metrics (admin) отображаются
- [ ] Quality metrics (admin) визуализируются
- [ ] Экспорт в PDF работает

### Admin Dashboard ✓
- [ ] Users table с фильтрами и поиском работает
- [ ] User details отображаются
- [ ] Ban/unban функциональность работает
- [ ] Specialists moderation работает (approve/reject/suspend)
- [ ] Reviews moderation работает (approve/reject)
- [ ] Bookings table с фильтрами работает
- [ ] Booking cancellation с возвратом работает
- [ ] Financial summary генерируется
- [ ] Specialist financials отображаются
- [ ] Platform stats работают

### Real-time Features ✓
- [ ] WebSocket подключение стабильно
- [ ] Chat отображает сообщения в реальном времени
- [ ] Chat typing indicator работает
- [ ] Chat read receipts работают
- [ ] Chat online status работает
- [ ] Notifications приходят в реальном времени
- [ ] Notification bell badge обновляется
- [ ] Desktop notifications работают
- [ ] Sound notifications работают
- [ ] Reconnection работает

### UI/UX ✓
- [ ] Все переиспользуемые компоненты работают
- [ ] Skeleton loaders на всех страницах
- [ ] Error states на всех страницах
- [ ] Empty states на всех страницах
- [ ] Toasts для всех действий
- [ ] Responsive design везде
- [ ] Анимации плавные
- [ ] Accessibility (aria-labels, keyboard navigation)

---

## Дополнительные требования

### Безопасность

- ✅ JWT токен хранится в localStorage
- ✅ Токен добавляется в каждый API запрос
- ✅ При 401 редирект на /login
- ✅ Protected routes для ADMIN/SPECIALIST/CLIENT
- ✅ XSS защита (sanitize user input)
- ✅ CSRF защита (если нужно)

### Performance

- ✅ Lazy loading для routes (React.lazy + Suspense)
- ✅ Code splitting по модулям
- ✅ Debounce для поиска (300ms)
- ✅ Throttle для scroll events
- ✅ Memoization для тяжелых вычислений (useMemo, React.memo)
- ✅ Виртуализация для длинных списков (react-window) - опционально

### Accessibility (a11y)

- ✅ Semantic HTML (header, nav, main, footer)
- ✅ Aria-labels для интерактивных элементов
- ✅ Keyboard navigation (Tab, Enter, Esc)
- ✅ Focus management в модальных окнах
- ✅ Color contrast (WCAG AA)

### SEO (опционально для MVP)

- ✅ Meta tags для каждой страницы
- ✅ Open Graph tags для соцсетей
- ✅ Structured data (JSON-LD)

---

## Заключение

Этот документ описывает полную интеграцию фронтенда Hearty.pro с новыми бэкенд API модулями.

**Общая оценка времени:** 4-5 недель при работе 1 разработчика full-time.

**Приоритизация:**
1. **Analytics Dashboard** (неделя 1-2) - высокая бизнес-ценность
2. **Admin Dashboard** (неделя 2-3) - операционная необходимость
3. **Real-time Features** (неделя 3-4) - конкурентное преимущество
4. **UI/UX Polish** (неделя 4-5) - качество продукта

**Следующие шаги:**
1. Утвердить ТЗ
2. Создать задачи в трекере (Jira, Notion, GitHub Projects)
3. Начать с Фазы 1 (Analytics Dashboard)
4. Регулярные ревью прогресса (weekly demos)
5. Тестирование на каждой фазе
6. Деплой на production после завершения каждой фазы

---

**Версия документа:** 1.0
**Дата:** 13 февраля 2025
**Автор:** Claude Code
**Проект:** Hearty.pro Frontend Specification
